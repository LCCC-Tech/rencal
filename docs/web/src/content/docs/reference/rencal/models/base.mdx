---
title: rencal.models.base
description: Reference documentation for rencal.models.base.
---

import Segment from '@/components/pydoc/Segment.astro';
import Section from '@/components/pydoc/Section.astro';

<div class="pydoc">

<Section type="about">
Reference for dataset base classes used by all rencal model containers.
</Section>

## Classes

### DatasetModel

<Segment>

```python
class DatasetModel(BaseModel, ABC)
```

Abstract base class defining shared validation/conversion contract.

#### Attributes

- `metadata: dict[str, Any] = {}`
- `required_columns: ClassVar[list[str]] = []`
- `required_datatypes: ClassVar[dict[str, str]] = {}`
- `date_column: ClassVar[str | None] = None`

#### Abstract Methods

##### _validate_required_columns

```python
_validate_required_columns(data: Any) -> Any
```

Validates required columns/variables are present.

**Parameters**
- `data (Any)`

**Returns**
- `Any`

##### _validate_datatypes

```python
_validate_datatypes(data: Any) -> Any
```

Validates required dtypes for fields configured by subclasses.

**Parameters**
- `data (Any)`

**Returns**
- `Any`

##### _validate_time_column

```python
_validate_time_column(data: Any) -> Any
```

Validates required time column/dimension when `date_column` is defined.

**Parameters**
- `data (Any)`

**Returns**
- `Any`

##### to_pandas

```python
to_pandas() -> pd.DataFrame
```

Converts the dataset to a pandas DataFrame representation.

**Returns**
- `pd.DataFrame`

##### to_xarray

```python
to_xarray() -> xr.Dataset
```

Converts the dataset to an xarray Dataset representation.

**Returns**
- `xr.Dataset`

</Segment>

### PandasDatasetModel

<Segment>

```python
class PandasDatasetModel(DatasetModel)
```

Concrete validator mixin for pandas-backed datasets.

#### Methods

##### _validate_required_columns

```python
_validate_required_columns(data: pd.DataFrame) -> pd.DataFrame
```

Checks `required_columns` against DataFrame columns and raises on missing fields.

**Parameters**
- `data (pd.DataFrame)`

**Returns**
- `pd.DataFrame`

**Raises**
- `ValueError`: If required columns are missing.

##### _validate_datatypes

```python
_validate_datatypes(data: pd.DataFrame) -> pd.DataFrame
```

Checks numeric and object/string-like expectations for configured columns.

**Parameters**
- `data (pd.DataFrame)`

**Returns**
- `pd.DataFrame`

**Raises**
- `ValueError`: If configured numeric dtype expectations are violated.

##### _validate_time_column

```python
_validate_time_column(data: pd.DataFrame) -> pd.DataFrame
```

Ensures time column exists, is datetime-like, non-empty, and null-free.

**Parameters**
- `data (pd.DataFrame)`

**Returns**
- `pd.DataFrame`

**Raises**
- `ValueError`: If time column checks fail.

</Segment>

### XArrayDatasetModel

<Segment>

```python
class XArrayDatasetModel(DatasetModel)
```

Concrete validator mixin for xarray-backed datasets.

#### Methods

##### _validate_required_columns

```python
_validate_required_columns(data: xr.Dataset) -> xr.Dataset
```

Checks required variables in `data.data_vars`.

**Parameters**
- `data (xr.Dataset)`

**Returns**
- `xr.Dataset`

**Raises**
- `ValueError`: If required variables are missing.

##### _validate_datatypes

```python
_validate_datatypes(data: xr.Dataset) -> xr.Dataset
```

Validates configured variable dtypes.

**Parameters**
- `data (xr.Dataset)`

**Returns**
- `xr.Dataset`

**Raises**
- `ValueError`: If variable dtype checks fail.

##### _validate_time_column

```python
_validate_time_column(data: xr.Dataset) -> xr.Dataset
```

Ensures time dimension/coordinate exists, is datetime-like, sorted, and duplicate-free.

**Parameters**
- `data (xr.Dataset)`

**Returns**
- `xr.Dataset`

**Raises**
- `ValueError`: If time dimension/coordinate integrity checks fail.

</Segment>

</div>
